using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitializeCurrentGameAudio : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        object bankID;
        //AkSoundEngine.LoadBank("Main.bnk", out bankID);
        //#AkSoundEngine.PostEvent("Music_Ambient_Drone",gameObject, gameObject);
        print("heheh");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
